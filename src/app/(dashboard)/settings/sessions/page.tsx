import SessionManagement from "@/features/auth/components/SessionManagement";

export const metadata = {
  title: "Quản lý phiên đăng nhập | FAMS",
};

export default function SessionsPage() {
  return <SessionManagement />;
}
